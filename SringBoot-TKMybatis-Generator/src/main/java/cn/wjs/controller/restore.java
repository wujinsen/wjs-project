package cn.wjs.controller;

public class restore {
    //        List<UserVo> resultList = list.stream().map(user->{
//            UserVo userVo = new UserVo();
//            BeanUtils.copyProperties(user, userVo);
//            if(repeatIds.contains(user.getUserId())){
//                String name = repeatMap.get(user.getUserId()) + "-"+user.getOrgFullNameList().toString();
//                repeatMap.put(user.getUserId(), name);
//            }else{
//                repeatMap.put(user.getUserId(), user.getOrgFullNameList().toString());
//            }
//            userVo.setOrgFullName(repeatMap.get(user.getUserId()));
//            repeatIds.add(user.getUserId());
//            return userVo;
//        }).collect(Collectors.toList());
//        Collections.reverse(resultList);
//        resultList = resultList.stream().filter(user->{
//            boolean flag = !repeatIdsTwo.contains(user.getUserId());
//            repeatIdsTwo.add(user.getUserId());
//            return flag;
//        }).collect(Collectors.toList());
//        pageInfo.setList(resultList);
}
